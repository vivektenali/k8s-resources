apiVersion: v1
kind: Pod
metadata:
  name: my-java-app
spec:
  containers:
  - name: java-app-container
    image: my-company/java-app:1.0
    # Wait for the application to finish starting
    startupProbe:
      httpGet:
        path: /health/startup
        port: 8080
      periodSeconds: 6
      failureThreshold: 30
    # Send traffic only when the app is ready
    readinessProbe:
      httpGet:
        path: /health/ready
        port: 8080
      periodSeconds: 10
      failureThreshold: 3
    # Restart the container if the app becomes unhealthy
    livenessProbe:
      httpGet:
        path: /health/live
        port: 8080
      periodSeconds: 10
      failureThreshold: 3